{
  "name": "frontend",
  "version": "1.0.0",
  "description": "JWT Auth Frontend",
  "main": "index.js",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}